package com.example.agripredict.ui.theme

import androidx.compose.ui.graphics.Color

// === Palette agricole AgriPredict ===

// Vert principal (nature, agriculture)
val GreenPrimary = Color(0xFF2E7D32)      // Vert foncé
val GreenPrimaryLight = Color(0xFF60AD5E) // Vert clair
val GreenPrimaryDark = Color(0xFF005005)  // Vert très foncé

// Marron secondaire (terre, sol)
val BrownSecondary = Color(0xFF795548)      // Marron
val BrownSecondaryLight = Color(0xFFA98274) // Marron clair
val BrownSecondaryDark = Color(0xFF4B2C20)  // Marron foncé

// Couleurs d'accent
val OrangeAccent = Color(0xFFFF8F00)  // Orange (soleil, alertes)
val YellowLight = Color(0xFFFFF8E1)   // Jaune pâle (fond)

// Couleurs de fond
val BackgroundLight = Color(0xFFF5F5F0) // Fond clair naturel
val SurfaceLight = Color(0xFFFFFFFF)    // Surface blanche
val BackgroundDark = Color(0xFF1B2E1B)  // Fond sombre naturel
val SurfaceDark = Color(0xFF2D3E2D)     // Surface sombre

// Couleurs de texte
val OnPrimaryLight = Color(0xFFFFFFFF)
val OnSecondaryLight = Color(0xFFFFFFFF)
val OnBackgroundLight = Color(0xFF1C1B1F)
val OnSurfaceLight = Color(0xFF1C1B1F)
val OnPrimaryDark = Color(0xFFFFFFFF)
val OnBackgroundDark = Color(0xFFE6E6E6)
val OnSurfaceDark = Color(0xFFE6E6E6)

// Couleurs sémantiques
val ErrorRed = Color(0xFFD32F2F)
val SuccessGreen = Color(0xFF388E3C)
val WarningOrange = Color(0xFFF57C00)
